import { RouteDirectionDirective } from './route-direction.directive';

describe('RouteDirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteDirectionDirective();
    expect(directive).toBeTruthy();
  });
});
